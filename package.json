{
  "name": "@pubcore/node-server-docker",
  "version": "1.4.1",
  "description": "node server composed with docker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "docker stack deploy -c docker-compose.yml -c d-compose-deploy.yml web",
    "stop": "docker stack rm web",
    "restart-app": "docker service scale web_app=0 -d && docker service scale web_app=1 -d"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "express-rate-limit": "^2.14.2",
    "helmet": "^3.21.1",
    "morgan": "^1.9.1"
  },
  "engines": {
    "node": ">=8.11.1",
    "docker": ">=18.06.0"
  },
  "devDependencies": {},
  "keywords": [
    "nodejs",
    "docker",
    "node",
    "server"
  ]
}
